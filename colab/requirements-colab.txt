# requirements-colab.txt — Colab T4 QLoRA fine-tuning dependencies
#
# torch is preinstalled on Colab (~2.3+); listed here for reference.
# Install with: pip install -r requirements-colab.txt
#
# Pinned to versions verified for SFTTrainer prompt/completion API,
# T4 (CC 7.5) fp16 support, and Qwen3.5-4B compatibility.
#
# IMPORTANT: Qwen3.5 requires transformers>=5.2.0 (qwen3_5 model_type
# first supported in transformers v5.2.0, released Feb 16 2025).
# transformers 4.x will fail with: ValueError: model type 'qwen3_5' not recognized

# Core training stack
transformers>=5.2.0
peft>=0.19.0
trl>=1.0.0
bitsandbytes>=0.49.0
accelerate>=1.4.0

# Data + tokenization
datasets>=4.7.0
sentencepiece>=0.2.0
tokenizers>=0.21.0

# Utilities
tqdm>=4.66.5
safetensors>=0.4.5

# Optional: install for faster Qwen3.5 linear-attention layers on GPU.
# Without these the model silently falls back to slower PyTorch ops (~2x slower).
# causal_conv1d @ https://github.com/Dao-AILab/causal-conv1d
# flash-linear-attention (fla) @ https://github.com/fla-org/flash-linear-attention

# torch: Colab preinstalls torch ~2.3+; do NOT reinstall unless version mismatch.
# If needed: pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu121
